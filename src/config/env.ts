// __DEV__ is true in Metro dev builds, false in release builds.
// Override with MOCK_MODE=false in env before switching to real backend.
export const MOCK_MODE: boolean = __DEV__;

export const MOCK_DELAY_MS = 600;
