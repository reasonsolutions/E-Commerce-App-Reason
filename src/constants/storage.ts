// Canonical re-export. New files should import from here.
// Existing screens import from '../config/storageKeys' and are not affected.
export { STORAGE_KEYS } from '../config/storageKeys';
export type { StorageKey } from '../config/storageKeys';
